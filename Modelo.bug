model{
  for(i in 1:n){
    y[i] ~ dbern(p[i])
    p[i] <- 1/(1.00001+exp(-(alpha+beta11*x1[i]+beta12*x2[i]+beta13*x3[i]+beta14*x4[i])))
  }
  alpha ~ dnorm(0.0, 0.0001)
  beta11 ~ dnorm(0.0, 0.0001)
  beta12 ~ dnorm(0.0, 0.0001)
  beta13 ~ dnorm(0.0, 0.0001)
  beta14 ~ dnorm(0.0, 0.0001)
}